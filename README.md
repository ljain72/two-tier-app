This is a helm chart for a simple two tier flask application. 
To use:
1. Clone the repo in local.
2. Connect to your cluster.
3. Run 'helm install <release-name> two-tier-app'.
4. values.yaml file can also be overwritted during install.
5. To uninstall 'helm uninstall <release-name>'
